# ☕ Coffee Bot — Mini App для сбора друзей на кофе в Telegram

Бот с **Mini App** (веб-приложением внутри Telegram): любой в группе открывает приложение, запускает сбор на кофе, выбирает время; остальные голосуют «Участвую» / «Не смогу»; инициатор подтверждает — бот напоминает в выбранное время.

---

## Как это работает

1. В группе кто-то пишет **/start** — бот присылает сообщение со **ссылкой** «☕ Сбор на кофе».
2. Нажатие на ссылку открывает **Mini App**: выбор времени (15 / 30 / 45 мин или 1 час) и создание сбора.
3. Остальные открывают то же приложение (ссылка в том же сообщении) и нажимают **«✅ Участвую»** или **«❌ Не смогу»**.
4. Когда все проголосовали, **инициатор** в приложении нажимает **«🔒 Подтвердить сбор»**.
5. Бот пишет в чат о подтверждении и в выбранное время отправляет: **«☕ Время! Идём за кофе.»**

---

## Деплой с GitHub (развернуть в интернете)

Репозиторий уже на GitHub — дальше разверните приложение на хостинге. Ниже два варианта (достаточно одного).

### Вариант 1: Railway
f
1. Зайдите на [railway.app](https://railway.app) и войдите через **GitHub**.
2. **New Project** → **Deploy from GitHub repo** → выберите репозиторий с coffee bot.
3. Railway сам определит Node.js и запустит `npm start`. Дождитесь первого деплоя.
4. В проекте откройте ваш **сервис** → вкладка **Variables** → **Add Variable**:
   - `BOT_TOKEN` = токен от @BotFather  
   - `BASE_URL` = пока не трогайте.
5. Вкладка **Settings** → секция **Networking** → **Generate Domain**. Скопируйте URL (например `https://coffee-bot-production-xxxx.up.railway.app`).
6. Вернитесь в **Variables** и задайте:
   - `BASE_URL` = этот URL **без слеша в конце** (например `https://coffee-bot-production-xxxx.up.railway.app`).
7. Сохраните переменные — Railway перезапустит приложение. Готово: бот и Mini App работают по этому адресу.

Порт на Railway задаётся автоматически, **PORT** в переменные добавлять не нужно.

### Вариант 2: Render

1. Зайдите на [render.com](https://render.com) и войдите через **GitHub**.
2. **New** → **Web Service** → подключите репозиторий с coffee bot.
3. Настройки:
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
   - **Instance Type:** можно оставить Free.
4. В блоке **Environment** добавьте переменные:
   - `BOT_TOKEN` = токен от @BotFather  
   - `BASE_URL` = пока оставьте пустым или временное значение.
5. Нажмите **Create Web Service**. После деплоя Render покажет URL вида `https://ваше-имя.onrender.com`.
6. В **Environment** измените `BASE_URL` на этот URL **без слеша в конце** и сохраните. Render перезапустит сервис.

Порт Render задаёт сам через переменную **PORT** — в коде уже используется `process.env.PORT`, ничего доп. настраивать не нужно.

### После деплоя

1. Добавьте бота в группу Telegram.
2. В группе отправьте **/start** — бот пришлёт сообщение со ссылкой **«☕ Сбор на кофе»**.
3. Нажмите на ссылку — откроется Mini App. Если открывается и показывается выбор времени, деплой прошёл успешно.

**Если сборка падает с ошибкой npm:** в логах видна только строка вида `A complete log of this run can be found in: ...`. Сама причина написана **выше** в том же логе деплоя (Railway: вкладка **Deployments** → нужный деплой → **View Logs**; прокрутите вверх до блока с `npm install`). Частые причины: таймаут сети, нехватка памяти. В проекте уже заданы `engines.node` и установка без optional зависимостей — после коммита и повторного деплоя часто помогает.

**Ошибка 409 (Conflict: getUpdates):** при заданном **BASE_URL** бот работает в режиме **webhook** (обновления приходят на ваш URL), а не polling — второй экземпляр не нужен, 409 не возникает. Убедитесь, что в логах при старте есть строка `Бот в режиме webhook: ...`.

**Ошибка BUTTON_TYPE_INVALID или старый текст («Нажмите, чтобы открыть приложение…»):** на сервере крутится **старая сборка**. Сделайте: закоммитьте и запушьте все изменения в GitHub; в Railway откройте **Deployments** → **Redeploy** (или сделайте пустой коммит и push). В логах после деплоя должно быть: `Бот в режиме webhook` и при /start в группе — сообщение **со ссылкой** «Нажмите ссылку, чтобы открыть приложение…», **без** кнопки под сообщением.

---

## Полная инструкция: как подключить в Telegram

### Шаг 1. Создать бота в BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram.
2. Отправьте команду **`/newbot`**.
3. Введите имя бота (например: `Coffee Collection`) и username (например: `my_coffee_collection_bot`). Username должен заканчиваться на `bot`.
4. BotFather пришлёт **токен** вида `123456789:ABCdefGHI...`. Скопируйте его — он понадобится для `.env`.

### Шаг 2. Установить проект и настроить .env

На компьютере в папке проекта:

```bash
cd "c:\Work\coffee bot"
npm install
```

Создайте файл **`.env`** (скопируйте из `.env.example` и подставьте свои значения):

```env
BOT_TOKEN=сюда_вставить_токен_от_BotFather
PORT=3000
BASE_URL=https://ваш-публичный-адрес.ваш-хостинг.app
```

- **BOT_TOKEN** — токен из шага 1.  
- **PORT** — порт сервера (можно оставить 3000).  
- **BASE_URL** — **обязательно** публичный **HTTPS**-адрес, по которому будет открываться Mini App (см. шаг 3).

### Шаг 3. Получить HTTPS-адрес для Mini App

Telegram открывает Mini App только по **HTTPS**. Варианты:

#### Вариант A: Локальная разработка (ngrok)

1. Установите [ngrok](https://ngrok.com/download).
2. В одном терминале запустите бота: `npm start`.
3. В другом: `ngrok http 3000`.
4. Скопируйте выданный HTTPS-URL (например `https://abc123.ngrok.io`) и вставьте в **BASE_URL** в `.env`. Перезапустите `npm start`.

#### Вариант B: Хостинг (Railway / Render / другой)

1. Залейте проект в Git (GitHub/GitLab).
2. Создайте приложение на [Railway](https://railway.app) или [Render](https://render.com):
   - Подключите репозиторий.
   - Укажите команду запуска: `npm start` (или `node src/index.js`).
   - Укажите порт из переменной **PORT** (часто хостинг сам задаёт PORT).
3. В настройках сервиса добавьте переменные окружения: **BOT_TOKEN**, **BASE_URL** (и при необходимости **PORT**).  
   **BASE_URL** — тот адрес, который выдаст хостинг (например `https://your-app.up.railway.app`). Без слеша в конце.
4. После деплоя скопируйте URL приложения и убедитесь, что он прописан в **BASE_URL** (в .env локально или в переменных хостинга).

### Шаг 4. Запустить бота

Локально:

```bash
npm start
```

В логах должно быть что-то вроде:  
`Сервер запущен на порту 3000` и `Mini App URL: https://...`.

### Шаг 5. Добавить бота в группу и открыть Mini App

1. Создайте группу в Telegram или откройте существующую.
2. Добавьте бота в группу (через «Добавить участников» → поиск по username бота).
3. В группе отправьте команду **`/start`**.
4. Под сообщением бота появится кнопка **«☕ Сбор на кофе»** — она открывает Mini App.
5. Нажмите кнопку: должно открыться приложение (выбор времени, голосование, подтверждение).

Если кнопка не открывает приложение или пишет ошибку — проверьте, что **BASE_URL** в `.env` (или на хостинге) совпадает с реальным HTTPS-адресом и что сервер запущен и доступен из интернета.

### Шаг 6. Main Mini App в BotFather (обязательно для кнопки в группе)

Чтобы кнопка «Сбор на кофе» в группе открывала приложение, нужно указать URL Mini App в BotFather:

1. Откройте [@BotFather](https://t.me/BotFather).
2. **`/mybots`** → выберите вашего бота.
3. **Bot Settings** → **Configure Mini App** (или **Configure Main Mini App** / **Enable Mini App**).
4. Включите Mini App и укажите **URL** = ваш **BASE_URL** (например `https://coffee-bot-production.up.railway.app`), без слеша в конце.

После этого в группе по **/start** бот пришлёт сообщение со ссылкой **«☕ Сбор на кофе»**; по нажатию откроется ваше приложение (параметр чата передаётся в ссылке). Документация: [Telegram Mini Apps](https://core.telegram.org/bots/webapps).

### Шаг 7. Кнопка «Открыть приложение» у бота в группе

При запуске с **BASE_URL** бот автоматически выставляет **кнопку меню** (как у BotFather «Open»): при нажатии на имя бота в группе или в личке появляется пункт **«Открыть приложение»**, по нажатию открывается Mini App. В группе приложение получит ID чата из контекста Telegram. Дополнительно можно настроить кнопку в BotFather: **Bot Settings** → **Menu Button**.

---

## Структура проекта

| Что | Описание |
|-----|----------|
| `src/index.js` | Запуск сервера и бота, чтение `.env` |
| `src/bot.js` | Логика бота и кнопка «Сбор на кофе» (открывает Mini App) |
| `src/server.js` | Express: раздача Mini App и API для сборов/голосов/подтверждения |
| `src/state.js` | Хранение активных сборов и таймеров (в памяти) |
| `public/index.html` | Страница Mini App |
| `public/app.js` | Логика Mini App (Telegram Web App API, вызовы API) |

---

## Требования

- Node.js 18+
- Групповой чат в Telegram (группа или супергруппа)
- Публичный **HTTPS**-адрес для Mini App (ngrok или хостинг)

Данные о сборах хранятся в памяти; после перезапуска приложения активные таймеры сбрасываются.
